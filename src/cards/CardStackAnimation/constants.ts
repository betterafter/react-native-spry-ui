export const CARD_GAP = 100;
export const DRAG_DISTANCE = 300;
export const SWIPE_THRESHOLD = 100;

export const VISIBLE_CARD_COUNT = 3;
export const PREFETCH_RANGE = 5;
export const WINDOW_OFFSETS = [-1, 0, 1, 2, 3] as const;

/** Each stack level behind the front card is narrower (inset per side). */
export const WIDTH_SHRINK_PER_LEVEL = 12;
